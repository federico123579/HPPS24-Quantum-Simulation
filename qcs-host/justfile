alias b := build
alias c := clean
alias d := doc
alias t := test

build:
    cargo build --release
    mv target/release/qcs-bins qcs

clean:
    rm -i ./qcs

doc:
    cargo doc

test:
    cargo nextest run && cargo test --doc
